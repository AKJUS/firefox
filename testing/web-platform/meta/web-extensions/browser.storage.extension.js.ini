[browser.storage.extension.html]
  expected:
    if (os == "linux") and asan and fission: [TIMEOUT, OK]
    if (os == "mac") and debug: OK
    if (os == "mac") and not debug: TIMEOUT
    if os == "android": ERROR
    [OK, TIMEOUT]
  [testStorageOnChanged]
    expected:
      if (os == "linux") and asan and fission: [NOTRUN, PASS]
      if (os == "linux") and asan and not fission: [PASS, NOTRUN]
      if (os == "mac") and debug: PASS
      if (os == "mac") and not debug: NOTRUN
      if (os == "linux") and not asan: [PASS, NOTRUN]
      [PASS, FAIL, NOTRUN]

  [testStorageSetAccessLevelTrustedContexts]
    expected: FAIL

  [testStorageSetAccessLevelTrustedAndUntrustedContexts]
    expected: FAIL
